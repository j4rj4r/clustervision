{{- if and .Values.namespace.create (not (lookup "v1" "Namespace" "" .Values.namespace.name)) }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace.name }}
  labels:
    {{- include "clustervision.labels" . | nindent 4 }}
{{- end }}
